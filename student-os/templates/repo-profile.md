# Repository Profile

## Purpose

- Repository type: student knowledge base
- Primary mode: markdown-first
- Git policy: semi-automatic, confirm before commit

## Canonical Paths

```yaml
paths:
  courses: courses
  projects: projects
  tasks: tasks
  reviews: reviews
  references: references
  dashboards: dashboards
```

## Legacy Mapping

Use this section when the repository already has stable custom folders.

```yaml
legacy_paths: {}
```

## Git Preferences

```yaml
git:
  branch_prefixes: [task, course, review, project, ops]
  commit_prefixes: [notes, course, review, project, tasks, ops]
  require_confirmation_before_commit: true
```
